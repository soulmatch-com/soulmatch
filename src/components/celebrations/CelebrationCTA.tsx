import Link from 'next/link'
import { ArrowRight } from 'lucide-react'
import { IndependentServiceNotice } from './IndependentServiceNotice'
export function CelebrationCTA({ title = 'Ready to begin planning?', href = '/plan', label = 'Start Planning' }: { title?: string; href?: string; label?: string }) {
  return <section className="bg-stone-900 py-14 text-white"><div className="mx-auto flex max-w-6xl flex-col items-start justify-between gap-7 px-4 sm:px-6 lg:flex-row lg:items-center"><div><p className="text-sm font-bold uppercase tracking-[0.2em] text-amber-400">MyThirumanam Celebrations</p><h2 className="mt-2 text-3xl font-bold">{title}</h2><p className="mt-3 max-w-2xl leading-relaxed text-stone-300">Tell us what your family needs. Requested services, availability and arrangements will be discussed before confirmation.</p><IndependentServiceNotice className="mt-4 max-w-2xl text-stone-400" /></div><Link href={href} className="inline-flex min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-amber-400 px-6 py-3 font-bold text-stone-950 transition hover:bg-amber-300 focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-white sm:w-auto">{label}<ArrowRight aria-hidden="true" className="h-5 w-5" /></Link></div></section>
}
