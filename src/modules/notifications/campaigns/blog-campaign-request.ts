// This serializable DTO is intentionally defined in the notification domain so
// a future transport can carry it without a Blog repository or framework object.
export type { BlogCampaignRequest } from './campaign-types.ts'
